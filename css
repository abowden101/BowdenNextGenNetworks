.reveal-text {
  opacity: 0;
  transform: translateY(30px);
  transition: all 1.2s cubic-bezier(0.16, 1, 0.3, 1); /* The "Premium" Ease */
}

.reveal-text.visible {
  opacity: 1;
  transform: translateY(0);
}
