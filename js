const canvas = document.getElementById('bg-canvas');
const ctx = canvas.getContext('2d');
// Create an array of 'stars' or 'nodes' that have x, y coordinates
// In your update loop, make them move slightly away or toward the mouse
// Use ctx.lineTo() to draw very thin (0.5px) lines between close nodes
